export class LibraryItemSpec {
    deployment_spec: {
        accept_all_EULA: boolean;
        default_datastore_id: string;
    };
    target: {
        folder_id: string;
        host_id: string;
        resource_pool_id: string;
    };

}
